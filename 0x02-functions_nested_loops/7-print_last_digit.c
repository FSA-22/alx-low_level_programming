#include "main.h"
/**
 * print_last_digit - function that prints the last digit of a number.
 *
 * Return: function will return 0.
 *
 * @n: int n i the function set of numbers.
 *
 * @x: int x assigned to last number.
 */
int print_last_digit(int)
{
	int n;
	int x;

	n = rand();
	x = n = % 10;
	printf("%d" n, x);
	return (0);
}
